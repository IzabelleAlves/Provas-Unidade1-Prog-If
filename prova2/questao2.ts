// 2. Transforme sua resposta da questão 1 em um subprograma que recebe como parâmetro a string e retorna o seu valor numérico.

function n(a: string): number {
  let s: number = Number(a);
  return s;
}

console.log(n("6"));
